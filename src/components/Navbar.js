import styled from "styled-components";

const Title = styled.h1`
  color: ${({ theme }) => theme.header};
  font-family: Toboggan;
`;
const Navbar = () => {
  return (
    <div>
      <Title>adjasffswewetrtrwyajh</Title>
    </div>
  );
};

export default Navbar;
